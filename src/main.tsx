import React from 'react';
import ReactDOM from 'react-dom/client';
// import App from './flow/app-flow';
import App from './app';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
